<h1>登録済みルーム一覧</h1>

<table>
  <thead>
    <tr>
      <th>ルーム画像</th>
      <th>ルーム名</th>
      <th>ルーム紹介</th>
      <th>料金</th>
      <th>作成日</th>
    </tr>
  </thead>
  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td><%= image_tag room.image.url %></td>
        <td>
          <%= link_to room.name, room_path(room.id) %>
        </td>
        <td><%= room.content %></td>
        <td>¥<%= room.price %></td>
        <td><%= room.created_at %></td>
      </tr>
    <% end %>
  </tbody>  
</table>